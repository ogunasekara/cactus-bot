{
  "name": "cactus-bot",
  "version": "1.0.0",
  "description": "## Overview",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "deploy-commands": "node deploy-commands.js",
    "pretty-lint": "npx eslint . --fix && npx prettier . --write",
    "lint": "npx eslint . --fix",
    "prettier": "npx prettier . --write"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.16.3"
  },
  "devDependencies": {
    "eslint": "^9.22.0",
    "jest": "^29.7.0"
  }
}
